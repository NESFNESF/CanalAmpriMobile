import { FilterWorkPipe } from './filter-work.pipe';

describe('FilterWorkPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWorkPipe();
    expect(pipe).toBeTruthy();
  });
});
